{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "tsc: build - client",
      "type": "shell",
      "command": "npx tsc",
      "args": [],
      "group": {
        "kind": "build",
        "isDefault": true
      },
      "problemMatcher": ["$tsc"],
      "options": {
        "cwd": "${workspaceFolder}"
      }
    }
  ]
}
